name: Dist (slim binaries)

# Every profile x platform, each running the full suite before anything is
# published (`release` needs: build). The size gate is tag-only so a legitimate
# growth lands in a PR job summary instead of blocking it.
#
# A profile added here costs four jobs at release time. The two alternative
# codegen modes therefore ship only with the minimum feature set instead of
# doubling all six smallest profiles.
#
# A pull request runs a reduced 4 x 2 matrix instead of the full 8 x 4; `gate`
# below emits both axes and documents what is dropped and why.

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: >-
          Publish the built binaries to a GitHub Release under this tag
          (e.g. slim-v1.2.3). The tag is created on the checked-out commit if it
          does not exist. Leave empty to only upload workflow artifacts.
        required: false
        type: string
      prerelease:
        description: 'Mark the release as a pre-release'
        required: false
        default: true
        type: boolean
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      # One tag publishes every profile; the per-profile spellings are accepted
      # too and simply name the profile someone cares about.
      - "slim-v[0-9]+.[0-9]+.[0-9]+*"
      - "slim-min-v[0-9]+.[0-9]+.[0-9]+*"
      - "slim-balanced-min-v[0-9]+.[0-9]+.[0-9]+*"
      - "slim-tuned-min-v[0-9]+.[0-9]+.[0-9]+*"
      - "slim-ffi-v[0-9]+.[0-9]+.[0-9]+*"
      - "slim-tls-v[0-9]+.[0-9]+.[0-9]+*"
      - "slim-sqlite-v[0-9]+.[0-9]+.[0-9]+*"
      - "slim-ffi-tls-v[0-9]+.[0-9]+.[0-9]+*"
      - "slim-ffi-tls-sqlite-v[0-9]+.[0-9]+.[0-9]+*"
  # Unfiltered on purpose -- the path filter lives in the `gate` job below,
  # because `on.pull_request` ANDs `paths` with `types` and cannot express
  # "these paths OR this label".
  pull_request:
    types: [ opened, synchronize, reopened, labeled ]

# Guarded on the event, not a flat `true`: `release` uploads assets, so a second
# tag push must never cancel a publish in flight.
concurrency:
  group: dist-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # Decides whether a pull_request event deserves the matrix at all, and how much
  # of it. Two ways in: the PR touches something that changes what gets built, or
  # it carries the `auto-release` label -- the upstream-release proposal, which
  # must have the full four-platform signal before it lands on `slim`, not
  # verify.yml's Linux-only six.
  #
  # Emitting the matrix from here rather than declaring it statically is what lets
  # a PR run 8 jobs where a tag runs 32; `fromJSON` on the two outputs below gives
  # the same two-axis cross product either way.
  gate:
    name: gate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      run: ${{ steps.decide.outputs.run }}
      combos: ${{ steps.decide.outputs.combos }}
      targets: ${{ steps.decide.outputs.targets }}
    steps:
      - name: Decide
        id: decide
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          EVENT: ${{ github.event_name }}
          PR: ${{ github.event.pull_request.number }}
          AUTO_RELEASE: ${{ contains(github.event.pull_request.labels.*.name, 'auto-release') }}
        run: |
          set -euo pipefail

          # The eight published profiles: six feature sets at `smallest`, plus the
          # two alternative codegen modes on the `min` feature set.
          FULL_COMBOS='[{"profile":"min","optimization":"smallest"},{"profile":"ffi","optimization":"smallest"},{"profile":"tls","optimization":"smallest"},{"profile":"sqlite","optimization":"smallest"},{"profile":"ffi-tls","optimization":"smallest"},{"profile":"ffi-tls-sqlite","optimization":"smallest"},{"profile":"min","optimization":"tuned"},{"profile":"min","optimization":"balanced"}]'
          FULL_TARGETS='[{"name":"linux-x86_64","os":"ubuntu-latest"},{"name":"linux-arm64","os":"ubuntu-24.04-arm"},{"name":"macos-arm64","os":"macos-15"},{"name":"windows-x86_64","os":"windows-latest"}]'

          # Floor, ceiling and both codegen modes. The four profiles left out are
          # feature unions of `min` and `ffi-tls-sqlite`, so a break in one of them
          # that does not also break an endpoint would have to be an interaction
          # between two optional features -- and verify.yml builds all six on Linux
          # for every push regardless.
          PR_COMBOS='[{"profile":"min","optimization":"smallest"},{"profile":"ffi-tls-sqlite","optimization":"smallest"},{"profile":"min","optimization":"tuned"},{"profile":"min","optimization":"balanced"}]'
          # Which is also why the Linux half of the platform axis goes: verify.yml
          # already covers it per push. These two are the toolchains it cannot --
          # Apple clang, the only one that actually gets -Oz and the machine
          # outliner, and MSVC, where six of the size/hardening levers compile out.
          PR_TARGETS='[{"name":"macos-arm64","os":"macos-15"},{"name":"windows-x86_64","os":"windows-latest"}]'

          decide() {
            { echo "run=$1"; echo "combos=$2"; echo "targets=$3"; } >> "$GITHUB_OUTPUT"
            echo "run=$1"
            exit 0
          }
          full() { decide true "$FULL_COMBOS" "$FULL_TARGETS"; }

          [ "$EVENT" = 'pull_request' ] || full

          if [ "$AUTO_RELEASE" = 'true' ]; then
            echo '::notice::auto-release PR -- running the full 8 x 4 matrix'
            full
          fi

          # `gh pr diff --name-only` would need a checkout; the API does not.
          gh api --paginate "repos/$REPO/pulls/$PR/files" --jq '.[].filename' > changed.txt
          if grep -qxE 'scripts/build-dist\.mjs|\.github/workflows/dist\.yml|CMakeLists\.txt|cmake/slim\.cmake' changed.txt; then
            echo '::notice::build-affecting PR -- reduced matrix: 4 profiles x macOS/Windows'
            decide true "$PR_COMBOS" "$PR_TARGETS"
          fi
          decide false "$PR_COMBOS" "$PR_TARGETS"

  build:
    name: ${{ matrix.combo.optimization == 'smallest' && matrix.combo.profile || format('{0}-{1}', matrix.combo.optimization, matrix.combo.profile) }} ${{ matrix.target.name }}
    needs: gate
    if: needs.gate.outputs.run == 'true'
    runs-on: ${{ matrix.target.os }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      # Both axes come from `gate`, which is what makes the PR matrix smaller than
      # the tag matrix. `combo` is one object rather than a profile/optimization
      # cross product plus ten `exclude` entries: the published set is a chosen
      # list, not a product, so enumerating it is both shorter and honest.
      matrix:
        combo: ${{ fromJSON(needs.gate.outputs.combos) }}
        target: ${{ fromJSON(needs.gate.outputs.targets) }}
    env:
      # `mise run` installs every tool in mise.toml first, and upstream publishes
      # no Linux asset for the `ubi:saghul/txiki.js` pin -- the Linux jobs would
      # die installing a binary the build never uses.
      MISE_DISABLE_TOOLS: 'ubi:saghul/txiki.js'
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      # Windows keeps setup-node: the mise task shell is POSIX sh, which the
      # Windows runner does not have.
      - name: Setup mise (Unix)
        if: runner.os != 'Windows'
        uses: jdx/mise-action@v3
        with:
          experimental: true

      - uses: actions/setup-node@v6
        if: runner.os == 'Windows'
        with:
          node-version-file: '.nvmrc'

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux' && contains(matrix.combo.profile, 'ffi')
        run: |
          sudo apt update
          sudo apt install -y libffi-dev

      # Every Windows profile, not just the FFI ones: the build step always
      # passes the vcpkg toolchain file, which must exist even when libffi
      # (the only package it provides) is not installed.
      - name: Setup vcpkg (Windows)
        if: runner.os == 'Windows'
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: '66c0373dc7fca549e5803087b9487edfe3aca0a1'

      - name: Install libffi (Windows)
        if: runner.os == 'Windows' && contains(matrix.combo.profile, 'ffi')
        env:
          VCPKG_DEFAULT_TRIPLET: x64-windows
          VCPKG_BINARY_SOURCES: "clear;x-gha,readwrite"
        run: vcpkg install libffi:x64-windows-static

      # esbuild comes from node_modules, not `npx` (network per run, and
      # version-unstable bundles).
      - name: Install npm dependencies
        run: npm install --ignore-scripts

      # --host-tjs also builds the full-CLI driver and the test fixtures into
      # build-host; the artifact cannot run the suite itself.
      - name: Build (Unix)
        if: runner.os != 'Windows'
        run: >-
          mise run dist -- --profile ${{ matrix.combo.profile }} --out dist --host-tjs
          --optimization ${{ matrix.combo.optimization }}
          ${{ startsWith(github.ref, 'refs/tags/') && '--enforce-size' || '' }}

      - name: Build (Windows)
        # node:util parseArgs refuses an option value that starts with a dash
        # unless it is written with `=`, so these cannot be split into two words.
        run: >
          node scripts/build-dist.mjs --profile ${{ matrix.combo.profile }} --out dist --host-tjs
          --optimization ${{ matrix.combo.optimization }}
          ${{ startsWith(github.ref, 'refs/tags/') && '--enforce-size' || '' }}
          "--cmake-arg=-DCMAKE_TOOLCHAIN_FILE=${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake"
          "--cmake-arg=-DVCPKG_TARGET_TRIPLET=x64-windows-static"
        if: runner.os == 'Windows'

      # The artifact's `test` subcommand is compiled out, so the host driver runs
      # the loop and TJS_TEST_EXE points every test file at the artifact. The
      # skip filter probes the artifact, so the tallies describe it, not the
      # driver; TJS_TEST_LIBDIR sends fixture lookups to the same tree.
      - name: Test the artifact
        shell: bash
        env:
          TJS_TEST_EXE: ${{ github.workspace }}/dist/tjs${{ runner.os == 'Windows' && '.exe' || '' }}
          TJS_TEST_LIBDIR: ./build-host
        run: |
          host=./build-host/tjs
          [ -x "$host" ] || host=./build-host/tjs.exe
          "$host" test tests/ 2>&1 | tee test-output.txt

      - name: Job summary
        if: always()
        shell: bash
        run: |
          # printf, not \x1b: that spelling is GNU-sed-only, and a sed matching
          # nothing would leave the tallies at zero -- which reads as "clean".
          esc=$(printf '\033')
          sed "s/${esc}\[[0-9;]*m//g" test-output.txt > test-plain.txt || : > test-plain.txt
          count() { grep -cE "[[:space:]]$1\$" test-plain.txt || true; }
          ok=$(count OK); skip=$(count SKIP); fail=$(count FAIL); timeout=$(count TIMEOUT)

          if [ -s test-output.txt ] && [ $((ok + skip + fail + timeout)) -eq 0 ]; then
            echo "::warning::the suite produced output but no result lines could be parsed"
          fi

          {
            echo "### ${{ matrix.combo.optimization == 'smallest' && matrix.combo.profile || format('{0}-{1}', matrix.combo.optimization, matrix.combo.profile) }} ${{ matrix.target.name }}"
            echo
            echo "| OK | SKIP | FAIL | TIMEOUT |"
            echo "| ---: | ---: | ---: | ---: |"
            echo "| $ok | $skip | $fail | $timeout |"
            echo
            echo '```'
            cat dist/BUILDINFO.txt || echo '(no BUILDINFO: the build failed)'
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - uses: actions/upload-artifact@v7
        with:
          name: txiki-slim-${{ matrix.combo.optimization == 'smallest' && matrix.combo.profile || format('{0}-{1}', matrix.combo.optimization, matrix.combo.profile) }}-${{ matrix.target.name }}
          path: dist

  # Every profile lands on one Release as txiki-slim-<profile>-<platform>.zip.
  # The `txiki-slim-` prefix keeps them from colliding with upstream
  # release.yml's `txiki-<platform>.zip`, which targets the same `v*` tags.
  release:
    name: release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || inputs.release_tag != ''
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
      - uses: actions/download-artifact@v8
        with:
          path: dist
          pattern: txiki-slim-*
          merge-multiple: false

      - name: Package
        run: |
          cd dist
          : > SHA256SUMS.txt
          for dir in */; do
            name="${dir%/}"
            # upload-artifact strips the executable bit; Info-ZIP preserves it.
            [ -f "${dir}tjs" ] && chmod +x "${dir}tjs"
            awk -v d="$name" '{ print $1 "  " d "/" $2 }' "${dir}SHA256SUMS" >> SHA256SUMS.txt
            zip -r "${name}.zip" "$dir"
          done
          cat SHA256SUMS.txt

      # VirusTotal may share uploads. Throttle free API requests.
      - name: Upload release packages to VirusTotal
        id: virustotal
        continue-on-error: true
        uses: crazy-max/ghaction-virustotal@936d8c5c00afe97d3d9a1af26d017cfdf26800a2 # v5.0.0
        with:
          vt_api_key: ${{ secrets.VIRUSTOTAL_API_KEY }}
          request_rate: 4
          files: dist/*.zip

      - name: Build size manifest
        env:
          RELEASE_TAG: ${{ inputs.release_tag || github.ref_name }}
        run: |
          node scripts/release-size-manifest.mjs collect \
            --input dist --tag "$RELEASE_TAG" --commit "$GITHUB_SHA" \
            --out dist/slim-sizes-v1.json

      - name: Release notes
        env:
          VT_ANALYSIS: ${{ steps.virustotal.outputs.analysis }}
        run: |
          {
            echo "Slim txiki.js builds (\`scripts/build-dist.mjs\`), eight profiles"
            echo "x four platforms."
            echo
            echo "| profile | codegen | FFI (\`tjs:ffi\`) | TLS (HTTPS/WSS/TLSSocket) | SQLite (\`tjs:sqlite\`) |"
            echo "| --- | --- | --- | --- | --- |"
            echo "| \`balanced-min\`   | \`-Oz\` + LTO, QuickJS at \`-Os\` | no  | no      | no  |"
            echo "| \`tuned-min\`      | \`-Oz\` + LTO, no outliner | no  | no          | no  |"
            echo "| \`min\`            | \`-Oz\` + LTO        | no  | no               | no  |"
            echo "| \`ffi\`            | \`-Oz\` + LTO        | yes | no               | no  |"
            echo "| \`tls\`            | \`-Oz\` + LTO        | no  | yes (bundled CA) | no  |"
            echo "| \`sqlite\`         | \`-Oz\` + LTO        | no  | no               | yes |"
            echo "| \`ffi-tls\`        | \`-Oz\` + LTO        | yes | yes (bundled CA) | no  |"
            echo "| \`ffi-tls-sqlite\` | \`-Oz\` + LTO        | yes | yes (bundled CA) | yes |"
            echo
            echo "The codegen column describes what is *requested*. \`-Oz\` and the"
            echo "AArch64 machine outliner are Clang-only, so on the Linux (GCC) and"
            echo "Windows (MSVC) artifacts all three \`min\` variants fall back to"
            echo "\`-Os\`/\`/O1\` + LTO and are the same binary. On macOS all three are"
            echo "distinct; see BUILDINFO.txt per artifact."
            echo
            echo "\`localStorage\` is persistent only on the SQLite profiles; the"
            echo "others fall back to an in-memory store."
            echo
            echo "All profiles use MinSizeRel, compressed bytecode, hardened (except MSVC,"
            echo "which compiles those levers out), no WebAssembly, no"
            echo "mimalloc, and no \`eval\`/\`serve\`/\`test\`/\`bundle\`/\`app\`"
            echo "subcommands. Always present: WebCrypto, \`run\`, the REPL, \`compile\`."
            echo
            echo "## VirusTotal scans"
            echo
            if [ -n "$VT_ANALYSIS" ]; then
              echo "| Package | Report |"
              echo "| --- | --- |"
              echo "$VT_ANALYSIS" | tr ',' '\n' | while IFS='=' read -r file url; do
                [ -n "$file" ] || continue
                echo "| \`$(basename "$file")\` | [analysis]($url) |"
              done
            else
              echo "_Scan unavailable for this release._"
            fi
            echo
            # The blank line after </summary> is what keeps the fenced blocks
            # inside parsing as Markdown rather than as literal text.
            echo '<details>'
            echo "<summary>see details, sha256, ... ($(ls -d dist/*/ | wc -l | tr -d ' ') builds)</summary>"
            echo
            for f in dist/*/BUILDINFO.txt; do
              echo '```'
              echo "$(basename "$(dirname "$f")")"
              cat "$f"
              echo '```'
            done
            echo '</details>'
          } > NOTES.md

      - uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ inputs.release_tag || github.ref_name }}
          name: ${{ inputs.release_tag || github.ref_name }} (slim builds)
          body_path: NOTES.md
          prerelease: ${{ github.event_name == 'workflow_dispatch' && inputs.prerelease }}
          files: |
            dist/*.zip
            dist/SHA256SUMS.txt
            dist/slim-sizes-v1.json
